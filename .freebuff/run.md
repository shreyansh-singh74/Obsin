# Obsin Dev Server

## Reproduce artifacts
- `npm install` (project uses npm based on package.json)
- No `.env.local` required for the landing page

## Run server
```bash
npx vite --host 0.0.0.0 --port 5173
```
The landing page is at `/` (root route via React Router).
